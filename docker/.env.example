# Copy to docker/.env and fill in. Used by `make uat`.

# Your Minecraft username — you'll be opped on join so you can /give and /fuse.
MC_USERNAME=YourMinecraftName

# Optional overrides:
# MC_VERSION=26.1.2
# MC_IMAGE=itzg/minecraft-server:java25
# MC_MEMORY=2G
# RCON_PASSWORD=fusion
